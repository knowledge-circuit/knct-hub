# Supply-chain age policy: 0 disables the check.
# Newer pnpm versions enforce a minimum age between a package's
# publish time and its install time. Off for now while the project
# is moving fast; revisit when stable.
minimum-release-age=0
